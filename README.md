Scripts:
  contiene tutti i file .gd per la logica di gioco.

models:
  contiene tutti gli assets grafici/audio.

native/concurrency_gate/src:
  contiene tutti i file .cpp che implementano le funzioni mutex e condition variables.


Il gioco è stato compilato per Debian.
